apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: demo-b
  namespace: app-demo
  labels:
    migration.nginx.org/poc: enabled
  annotations:
    nginx.ingress.kubernetes.io/proxy-body-size: "10m"
spec:
  ingressClassName: nginx
  rules:
    - host: demo-b.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: demo-svc
                port:
                  number: 80
